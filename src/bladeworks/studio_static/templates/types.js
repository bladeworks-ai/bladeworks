/**
 * Template-only view-model types.
 *
 * These are the extra bags of fields that HTML functions need on top of
 * EditorState. They are not the editor's canonical model; that stays in types.ts.
 */
export {};
